package java8.notes;

public class BehaviorParameterizationNotes {

	/*
	 * IMPORTANTE NOTE 1
	 * 
	 * The Java API contains many methods that can be parameterized with different behaviors:
	 *  - Sorting (Comparator) 
	 *  - Threads (Runnable)
	 *  - GUI handling (Event Handler).
	 */
}
